import 'package:flutter/material.dart';
import 'package:mockup_app/exercicios/gerador_de_cpf/src/view/page_view.dart';

class GeradorDeCpf extends StatelessWidget {
  const GeradorDeCpf({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: HomePage(),
    );
  }
}
